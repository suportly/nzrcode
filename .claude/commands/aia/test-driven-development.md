---
description: Write the failing test before the implementation, always.
allowed-tools: Read, Write, Edit, Glob, Grep, Bash
argument-hint: "<feature or fix>"
---

Invoke the `test-driven-development` skill. Red → green → refactor. Never write
implementation first.

Target: $ARGUMENTS
